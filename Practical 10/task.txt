/*
NAME: NIDHI MANGTANI (23070521098)
SUBJECT: JAVASCRIPT
PRACTICAL 10 – FINAL TASK (B1)
--------------------------------------------------
TITLE: Product Inventory Dashboard using Fetch and jQuery AJAX
--------------------------------------------------

AIM:
To create a dynamic **Product Inventory Dashboard** that fetches data from a local
`products.json` file using both **Fetch API** and **jQuery AJAX ($.getJSON())**, displays it
in tabular format, and provides live interactivity such as filtering, searching, and
visual highlighting.

--------------------------------------------------
PROBLEM STATEMENT:
--------------------------------------------------
Direct file modification is restricted in client-side JavaScript due to browser security policies.
Therefore, instead of inserting directly into JSON files, we simulate dynamic data handling 
through fetching, displaying, and filtering the existing product data in real time.

The dashboard must:
1. Load and display data from `products.json` using both Fetch API and jQuery AJAX.
2. Allow users to filter products by category using a dropdown.
3. Allow search functionality for product names (case-insensitive).
4. Highlight low-stock items (stock < 5) in red.
5. Display the count of currently visible products in the browser console.
6. Work entirely on the client-side with HTML, CSS, and JavaScript.

--------------------------------------------------
FILES REQUIRED:
--------------------------------------------------
1. **index.html** → Main dashboard interface  
2. **script.js** → Logic for fetching, displaying, filtering, and searching data  
3. **products.json** → Sample dataset (must have at least 5 product records)

--------------------------------------------------
SAMPLE products.json DATA:
--------------------------------------------------
[
  { "id": 1, "name": "Laptop", "category": "Electronics", "price": 55000, "stock": 8 },
  { "id": 2, "name": "Mouse", "category": "Electronics", "price": 1200, "stock": 3 },
  { "id": 3, "name": "Chair", "category": "Furniture", "price": 3000, "stock": 2 },
  { "id": 4, "name": "Notebook", "category": "Stationery", "price": 100, "stock": 10 },
  { "id": 5, "name": "Table", "category": "Furniture", "price": 7000, "stock": 4 }
]

--------------------------------------------------
TASK REQUIREMENTS:
--------------------------------------------------
1️⃣ **Fetch API Table**
   - Use `fetch('products.json')`
   - Parse JSON data and dynamically render a table with columns:
     [ID, Name, Category, Price, Stock]
   - Highlight rows in red where stock < 5.

2️⃣ **jQuery AJAX Table**
   - Use `$.getJSON('products.json')`
   - Render the same data in another table for comparison.

3️⃣ **Category Filter**
   - Add a dropdown listing all product categories.
   - On selection, show only products belonging to that category.
   - Reset option → show all products again.

4️⃣ **Search Functionality**
   - Add a text box for searching by product name.
   - Filter displayed items live as user types (case-insensitive).
   - Filtering should occur on the **already fetched array** (no refetching).

5️⃣ **Visual & Console Output**
   - Rows with low stock (stock < 5) → highlighted in red.
   - Console should log:  
     `"Currently displaying X products"` whenever the list updates.

--------------------------------------------------
CHALLENGE CONSTRAINTS:
--------------------------------------------------
- No page reloads allowed.
- Search and filter logic must operate on the in-memory product array.
- Low stock highlighting must use JavaScript DOM manipulation, not CSS filters.
- The final implementation must show:
  - One table rendered with Fetch API
  - Another rendered with jQuery $.getJSON()

--------------------------------------------------
EXPECTED USER INTERFACE:
--------------------------------------------------
| ID | Name      | Category     | Price | Stock |
|----|------------|--------------|--------|-------|
| 1  | Laptop     | Electronics  | 55000  | 8     |
| 2  | Mouse      | Electronics  | 1200   | 3 🔴 |
| 3  | Chair      | Furniture    | 3000   | 2 🔴 |
| 4  | Notebook   | Stationery   | 100    | 10    |
| 5  | Table      | Furniture    | 7000   | 4 🔴 |

- Dropdown → Category selection (All, Electronics, Furniture, Stationery)
- Search box → Filters by product name instantly
- Red-highlighted rows indicate low-stock items

--------------------------------------------------
CONSOLE OUTPUT EXAMPLE:
--------------------------------------------------
"Currently displaying 5 products"
"Currently displaying 3 products (after filtering)"
"Currently displaying 1 product (search: 'Mouse')"

--------------------------------------------------
END OF TASK DESCRIPTION
--------------------------------------------------
*/
